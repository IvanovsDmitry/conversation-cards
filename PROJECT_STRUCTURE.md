# Структура проекта "Разговорные карты"

## 📁 Файловая структура

```
ConversationCards/
├── ConversationCardsApp.swift      # Точка входа приложения
│
├── Models/
│   ├── Card.swift                 # Модель карты с вопросами
│   └── Deck.swift                 # Модель колоды карт
│
├── Views/
│   ├── DeckListView.swift         # Главный экран со списком колод
│   ├── CardViewerView.swift       # Экран просмотра карт с flip-анимацией
│   ├── AddDeckView.swift          # Экран добавления новой колоды
│   └── EditDeckView.swift         # Экран редактирования колоды и карт
│
├── Managers/
│   └── DeckManager.swift          # Менеджер для сохранения/загрузки колод
│
└── Data/
    └── BuiltInDecks.swift         # 6 встроенных колод с вопросами
```

## 🎯 Основные компоненты

### Модели данных

**Card.swift**
- `id: UUID` - уникальный идентификатор
- `mainQuestion: String` - основной вопрос
- `additionalQuestion: String` - дополнительный вопрос

**Deck.swift**
- `id: UUID` - уникальный идентификатор
- `name: String` - название колоды
- `emoji: String` - эмодзи для визуализации
- `colorHex: String` - цвет колоды в HEX
- `cards: [Card]` - массив карт
- `isBuiltIn: Bool` - флаг встроенной колоды
- `ageRating: String?` - возрастной рейтинг (опционально)

### Менеджеры

**DeckManager.swift**
- Управление всеми колодами
- Сохранение в UserDefaults
- Загрузка при старте приложения
- CRUD операции для колод и карт

### Встроенные колоды

**BuiltInDecks.swift**
1. Компания друзей 16+ (🎉) - 10 карт
2. Дети 5+ (🌈) - 10 карт
3. Большая семья (👨‍👩‍👧‍👦) - 10 карт
4. Для пар (💑) - 10 карт
5. Лучшие друзья (🤝) - 10 карт
6. Пары 18+ (🔥) - 10 карт

### Экраны

**DeckListView**
- Сетка колод (2 колонки)
- Кнопка добавления новой колоды
- Навигация к экрану просмотра карт

**CardViewerView**
- Просмотр карт с навигацией
- Flip-анимация при перевороте
- Свайп для переключения
- Кнопка случайной карты
- Кнопка редактирования

**AddDeckView**
- Форма добавления колоды
- Выбор эмодзи и цвета
- Текстовый ввод вопросов
- Парсинг формата "вопрос || дополнительный вопрос"

**EditDeckView**
- Список карт в колоде
- Редактирование карт
- Добавление новых карт
- Удаление карт (свайп влево)

## 🔧 Технические детали

### Хранение данных
- Используется `UserDefaults` для локального хранения
- Данные сериализуются в JSON через `Codable`
- Автоматическое сохранение при изменениях

### Анимации
- Flip-анимация карты (spring animation)
- Плавные переходы между картами
- Fade-эффекты для UI элементов

### Навигация
- `NavigationView` для основной навигации
- `NavigationLink` для переходов
- `sheet` для модальных окон

### Совместимость
- iOS 14.0+
- SwiftUI
- Поддержка тёмной/светлой темы (автоматически)

## 🎨 Дизайн

- Минималистичный стиль в духе Telegram
- Цветовая схема для каждой колоды
- Адаптивная типографика
- Плавные тени и скругления
- Системные цвета для поддержки тем

## ✨ Особенности

1. **Flip-анимация** - карта переворачивается при нажатии
2. **Свайп-навигация** - переключение карт жестом
3. **Случайная карта** - кнопка для непредсказуемости
4. **Редактирование** - полный CRUD для колод и карт
5. **Импорт колод** - добавление через текстовый ввод
6. **Автосохранение** - все изменения сохраняются автоматически

## 📱 Использование

1. Запустите приложение
2. Выберите колоду на главном экране
3. Просматривайте карты, переключаясь стрелками или свайпом
4. Нажмите на карту для переворота и просмотра дополнительного вопроса
5. Используйте кнопку редактирования для изменения колоды
6. Добавляйте свои колоды через кнопку "+"

